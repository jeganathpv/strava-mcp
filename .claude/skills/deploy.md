---
description: Deploy the Cloudflare Worker using wrangler. Run `npm run deploy` in the current working directory.
---

Run `npm run deploy` in the current working directory to deploy the Cloudflare Worker via wrangler. Show the output to the user.
